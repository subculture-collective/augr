name: Security

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 8 * * 1' # Weekly Monday 8am UTC

permissions:
  contents: read

jobs:
  go-dependency-audit:
    name: Go Dependency Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Check for known vulnerabilities
        run: |
          # v1.8 requires Go 1.26; keep the scanner compatible with the Go 1.25 target.
          go install golang.org/x/vuln/cmd/govulncheck@v1.7.0
          govulncheck ./...

      - name: Verify go.sum integrity
        run: go mod verify

      - name: Check for unused dependencies
        run: |
          go mod tidy
          git diff --exit-code go.mod go.sum || (echo "::error::go.mod/go.sum not tidy — run 'go mod tidy'" && exit 1)

  secret-scan:
    name: Secret Detection
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: TruffleHog secret scan
        run: |
          version=3.97.0
          archive="trufflehog_${version}_linux_amd64.tar.gz"
          release="https://github.com/trufflesecurity/trufflehog/releases/download/v${version}"
          curl --fail --silent --show-error --location --output "$archive" "$release/$archive"
          curl --fail --silent --show-error --location --output checksums.txt "$release/trufflehog_${version}_checksums.txt"
          grep "  $archive\$" checksums.txt | sha256sum --check --strict -
          tar --extract --gzip --file "$archive" trufflehog
          ./trufflehog git "file://$PWD" --only-verified --fail --no-update

  docker-scan:
    name: Docker Image Scan
    runs-on: ubuntu-latest
    if: github.server_url == 'https://github.com'
    steps:
      - uses: actions/checkout@v6

      - name: Build image
        run: docker build -t tradingagent:scan .

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: tradingagent:scan
          format: sarif
          output: trivy-results.sarif
          severity: CRITICAL,HIGH

      - name: Upload Trivy scan results
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: trivy-results.sarif

  npm-audit:
    name: Frontend Dependency Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: npm audit
        working-directory: web
        run: npm audit --audit-level=high || true
