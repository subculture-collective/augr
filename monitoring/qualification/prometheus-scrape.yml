# Staged fragment: merge scrape_configs into Dozor's managed configuration.
# Do not replace the fleet Prometheus configuration with this standalone file.
global:
  scrape_interval: 15s
scrape_configs:
  - job_name: augr-api
    metrics_path: /metrics
    static_configs:
      - targets: ['10.0.0.56:3030']
